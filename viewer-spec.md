# X ブックマーク・ローカルビューア 仕様書

Claude Code への実装依頼用。この仕様書と一緒に、実データの CSV（`twitter-フ_ックマーク-*.csv`）と参考実装（`x-bookmarks-viewer.html`）を渡すこと。参考実装はパース仕様の動く定義として扱ってよい。

## 1. 目的

twitter-web-exporter がエクスポートした X（旧Twitter）ブックマークの CSV を、ローカル環境で長期保管・快適に閲覧するためのアプリを作る。役割分担として、Raindrop.io は「タグ付けと横断検索の索引」、本アプリは「本文・メディアを読み返す書庫」。したがって読みやすさと検索性を最優先し、X 本体や外部サービスに依存しない。

## 2. 入力データ仕様（最重要）

ソースは twitter-web-exporter (prinsss) の Bookmarks CSV エクスポート。**UTF-8 BOM 付き**、カンマ区切り、全22列。

| 列 | 型 | 備考 |
|---|---|---|
| id | string | ツイートID。**重複排除の主キー**（url ではなく id を使う） |
| created_at | string | `2026-08-16 10:30:17 +09:00` 形式。**下記の罠①参照** |
| full_text | string | 本文。**罠②参照** |
| media | JSON文字列 | `[{"type","url","thumbnail","original"}]` の配列。空は `[]`。罠③参照 |
| screen_name | string | @なしのハンドル |
| name | string | 表示名 |
| profile_image_url | string | アバターURL（404になり得る） |
| user_id | string | 数値ID |
| in_reply_to | string | 返信先ツイートの**裸のID**（本文は含まれない）。ほぼ空 |
| retweeted_status | string | 観測データでは全行空。オプショナル扱い |
| quoted_status | string | 引用元ツイートの**裸のID**（本文は含まれない）。約3割の行に存在 |
| media_tags | JSON文字列 | 通常 `[]` |
| favorite_count 〜 views_count | int文字列 | views_count は空のことがある |
| favorited / retweeted / bookmarked | string | `"TRUE"` / `"FALSE"` の文字列 |
| url | string | `https://twitter.com/...` 形式。**x.com に正規化する** |

### データの罠（実データで確認済み。ここを外すと使い物にならない）

1. **created_at のタイムゾーン**: `+09:00` がスペース区切りで付く独自形式。`new Date()` に直接渡さず明示的にパースする。表示は**元のオフセット（JST）のまま**行い、閲覧環境のタイムゾーンに変換しない。ソート用に epoch 秒も保持する。
2. **full_text のエスケープ**: 改行が実際の改行ではなく**リテラル2文字の `\n`**（`\t` も同様）で入っている。表示前にアンエスケープ必須。3連以上の改行は2つに圧縮。
3. **media と t.co の関係**: 本文末尾の t.co リンクのうち、media 配列の `url` と一致するものは**メディア自身を指すだけなので本文から除去**する。実体は `original`（原寸）を使い、`thumbnail` はサムネイル用。`type` は `photo` / `video` / `animated_gif`。video の `original` は mp4 直リンク。一致しない t.co は外部リンクなので残す（短縮の展開はスコープ外）。
4. **引用・返信の扱い（CSV と JSON で異なる）**: CSV には引用元の**裸の ID しか入らない**ため本文は復元不能で、`https://x.com/i/status/{id}` へのリンク＋プレースホルダ表示になる。一方、**JSON エクスポートで「Include all metadata」を有効にすると、各レコードの `metadata` に GraphQL の生 Tweet オブジェクトが丸ごと入り、`metadata.quoted_status_result.result` に引用元の全文・作者・メディアが含まれる**（エクスポーターのソースコード `src/components/modals/export-data.tsx` と `src/types/tweet.ts` で確認済み）。引用の入れ子表示はこの JSON 経由で実現する。抽出時の注意: 本文は `note_tweet.note_tweet_results.result.text ?? legacy.full_text`（長文対応）、メディアは `legacy.extended_entities.media` 優先、動画は `video_info.variants` から `video/mp4` の最大ビットレートを選ぶ、`TweetWithVisibilityResults` は `.tweet` を1段剥がす、`TweetTombstone` / `TweetUnavailable` は「削除済み・非公開」表示にする。ユーザーの `screen_name` / `name` は `user.legacy` 配下（新 API では `user.core` 配下の場合あり、両対応）。
5. **メディアの死**: 元ツイート削除で画像が 404 を返す。壊れた画像は枠ごと非表示にする（onerror 等）。
6. **重複**: 同一 CSV 内の重複はないが、**エクスポートを重ねると期間が被る**。id で突合し、新規のみ追加。

### アイテムが持つ「手作業の資産」

取り込んだ内容とは別に、ユーザーが手で付けた情報を同じレコードに載せる。**この3種はどのマージ経路でも壊してはならない。**

| フィールド | 意味 |
|---|---|
| `c` / `cAt` | フォルダ割当と分類時刻（単一割当、未分類 = `''`） |
| `tr` | ゴミ箱に入れた時刻（0 = 通常表示。データは消さない） |
| `n` / `nAt` / `nEx` | メモ本文・最終更新時刻・最後にエクスポートした時刻（0 = 未出力） |

マージ規則は資産の性質で分ける。**`c` と `tr` は「既存優先」**（一度分類したものを外から上書きさせない）、
**`n` は「`nAt` が新しい方が勝つ」**（メモは繰り返し編集されるため）。`nEx` は両者の `Math.max` を取る。

参考規模: 実データは 636 件・475KB・期間 2011〜2026 年。本文中央値 69 字、最長 1,913 字。メディア付き 360 件、引用付き 186 件。

## 3. 機能要件

### MVP

1. **インポート**: CSV / JSON をドラッグ&ドロップまたはファイル選択で複数取込。id でマージし「新規 n 件 / 重複 m 件」を報告。CSV は列順に依存せずヘッダー名で列を特定。JSON は3種を自動判別する: (a) エクスポーターの通常 JSON（トップレベルが配列、フィールドは CSV と同名）、(b) 同 JSON ＋ metadata（各要素に `metadata` あり → 引用データ `qd` を抽出して保存。**既存アイテムに対しては `qd` のみ補完し、フォルダ割当を壊さない**）、(c) 本アプリのバックアップ（`items` を持つオブジェクト）。metadata 自体は巨大なので永続化せず、抽出した `qd` だけを保存する。
2. **永続化**: ローカル保存（IndexedDB 推奨。数万件を想定）。取込履歴（ファイル名・件数・日付）も保持。全削除機能。
3. **一覧**: 新しい順がデフォルト。1万件でも引っかからないこと（仮想スクロールまたは段階描画）。
4. **検索**: 本文・表示名・ハンドルの部分一致インクリメンタル検索。
5. **絞り込み**: メディアあり / 引用あり / 長文（300字以上）/ 特定ユーザー（カード上のハンドルをクリックで発動）。
6. **ソート**: 日付昇降、いいね数。
7. **カード表示**: 改行保持、URL 自動リンク、ハッシュタグ強調、`元の投稿` / `返信先` への外部リンク。**動画・GIF はクリックでその場再生**（`media.original` の mp4 直リンクを `<video>` で再生。GIF は loop + muted。読み込み失敗時は「削除済みの可能性」フォールバックと直リンクを表示。サムネイルが 404 でも再生枠自体は消さない）。**引用ポストは入れ子カードで表示**: 優先順に (a) アイテム自身が引用データ（`item.qd`）を持てばそれを描画、(b) 引用元 ID が書庫内にあれば相互参照して描画、(c) どちらも無ければ「書庫外・本文未取得」プレースホルダ＋外部リンク。入れ子は1段まで（再帰しない）。
8. **フォルダ（カテゴリ）**: ユーザー定義のフォルダに各アイテムを**単一割当**（`item.c = フォルダ名`、未割当 = 未分類）。初期値は「知識系 / 面白系 / あとで読む」。追加・改名・削除が可能で、削除時は中のアイテムを未分類に戻す。改名はアイテム側の割当も追随。ヘッダーにフォルダタブ（すべて / 未分類 / 各フォルダ、件数バッジ付き）。カード上でワンタップ割当・解除。
9. **整理モード（トリアージ）**: 未分類のアイテムを1件ずつ全画面表示し、数字キー 1〜9 でフォルダ割当、Enter でスキップ、← で直前の操作を取り消し、Esc で終了。進捗（n / 総数）を常時表示。マウス用ボタンも併設。フォルダ割当こそが本アプリの主目的なので、この画面の操作効率を最優先する。
10. **バックアップ / 復元**: 書庫全体（items・フォルダ定義・割当・取込履歴）を JSON でダウンロード。同じ JSON をドロップすると復元。復元時は id で突合し、新規は追加、既存には割当（`c`）とメモ（`n`）を適用。**フォルダ割当とメモは手作業の資産**なので、CSV 再取込・マージで消えないことを保証する。
11. **メモ**: 1アイテムに短いメモ（`n`）をカード上で直接書ける。フォルダとは別レイヤーで、割当の有無に関わらず付けられる。blur / `Ctrl`・`⌘`+`Enter` で保存、`Esc` で取消。保存時にリスト全体を再描画しない（他に開いている入力欄を消さないため）。ヘッダに「メモ」絞り込み、検索対象にもメモ本文を含める。整理モードにはメモ欄を出さない（割当の操作効率を落とさないため）。
12. **Obsidian（`40_Materials`）へのエクスポート**: メモを書いたアイテムを、Vault 運用マニュアル §11 の Materials 追記型に合わせた**プレーンテキスト**でまとめ、クリップボードにコピー／`.md` でダウンロードする。書式は §3-1 参照。
    - 出力対象は「未出力のみ（既定）/ 出力後に編集したもの / すべて」＋「いま絞り込んでいる範囲だけ」。ゴミ箱は常に除外
    - 並び順は「メモを書いた順（古い順、既定）/ 投稿が新しい順」。追記ファイルなので時系列が既定
    - **出力してもメモは消さない**（§11「使ったものには行末に印を付ける。消さない」）。コピー／ダウンロードが成功したときだけ `nEx` に出力日を記録し、既定の対象から外す。出力後にメモを編集すると `nAt > nEx` になり「出力後に編集したもの」として再び出せるので、**思考の変化だけを追記できる**
    - 誤操作に備え「直前の印を取り消す」を用意する（セッション中のみ）
    - 設定（対象・並び順・引用字数・印を付けるか）は永続化する

### 3-1. エクスポート書式（Materials 追記型）

```
08-22 蒸留の話、教師モデルの出力を全部使わず上位k個に絞る手法。
      うちのケースでも隠れ層を絞る前にこれを試す余地がある
      > 知識蒸留で全ロジットを使うのは実は無駄が多くて、上位k個に絞っても精度は…
      @someone https://x.com/someone/status/1001
```

- 日付は**メモを書いた日**（`nAt`）。ツイートの投稿日ではない。出力時と同じ年なら `MM-DD`、違う年なら `YYYY-MM-DD`
- 1行目は `MM-DD ` ＋ メモ1行目。メモの2行目以降と原文・出典は**半角6スペース**字下げ（`MM-DD ` と同じ幅）
- 原文は既定150字（設定可、0 で行ごと省略）。改行は空白に潰し、切り詰めたときだけ末尾に `…`。**符号位置で数え、絵文字を割らない**
- 本文が空でメディアだけのポストは `> （本文なし・メディアのみ）`
- 出典行は `@{screen_name} {url}`（url は取込時点で x.com 正規化済み）。エントリ間は空行1つ

### 拡張（優先度順、MVP後）

- **AI 自動分類**: Anthropic API 等に本文をバッチで渡し、既存フォルダへの割当案を生成 → ユーザーが整理モードで承認/修正する半自動フロー。API キーはユーザー入力とし、保存する場合は平文で永続化しない
- 統計ビュー（ユーザー別件数、月別推移）
- アイテムへの複数タグ付与（メモは MVP 11 で実装済み。タグはフォルダとは別レイヤー）
- エクスポートの `40_Materials/2026.md` への直接追記（File System Access API。現状はクリップボード / `.md` ダウンロードまで）
- Raindrop.io 向け CSV エクスポート（列: `folder,url,title,note,tags,created`。title は `表示名 (@handle): 本文冒頭150字`、note に本文全文とメディアURL、created は epoch 秒）
- メディアのローカル保存（twitter-web-exporter の「Export Media」zip との紐付け）

## 4. 非機能要件

- **ローカルファースト**: 外部通信は twimg.com からの画像取得のみ。テレメトリなし。アカウント不要。
- テキスト閲覧はオフラインで完結（メディアのみ要ネットワーク）。
- キーボード操作可、`prefers-reduced-motion` 尊重、モバイル幅対応。

## 5. 技術方針（提案。最終判断は実装側に委ねる）

Vite + 任意の軽量フレームワーク + IndexedDB（Dexie 等）の SPA を第一候補とする。配布を1ファイルで済ませたい場合は vite-plugin-singlefile 等でのシングル HTML 化を検討。デスクトップアプリ（Tauri）はメディアのローカル保存まで踏み込む場合のみ。**サーバーやDBを別プロセスで立てる構成は不可**（ダブルクリックで開ける手軽さを維持）。

## 6. 受け入れ基準（添付の実CSVで検証）

1. 636 件すべて取り込まれ、件数表示が一致する
2. 同じ CSV を再取込すると「新規 0 件」になる
3. id `2088366444006522946`（1,913字の長文）が改行構造を保って全文読める
4. 本文からメディア自身の t.co が消え、原寸メディアへのリンクが出る
5. 引用付き 186 件に「引用元」リンクが付く
6. 日付が閲覧環境のタイムゾーンに関係なく JST 表記で出る
7. `twitter.com` の URL が `x.com` に正規化される
8. 検索「ポケモン」で 2 件ヒットする
9. ブラウザのローカルストレージ類が使えない環境でも、クラッシュせず「保存不可」の警告表示にフォールバックする
10. 任意のアイテムをフォルダに割当 → 再読込しても割当が残る → 同じ CSV を再取込しても割当が消えない
11. 整理モードで「1キー→1キー→Enter→←」と操作すると、割当2件・スキップ1件・スキップ取消1件として正しく反映される
12. バックアップ JSON を書き出し、書庫を初期化してから同 JSON をドロップすると、件数・フォルダ定義・割当がすべて復元される
13. metadata 付き JSON（GraphQL 形状）を取り込むと、引用元の作者・本文・メディアが入れ子カードで表示され、`TweetTombstone` は「削除済み」表示になる
14. 動画サムネイルをクリックすると最大ビットレートの mp4 が `<video>` でその場再生され、取得失敗時はフォールバック表示になる
15. CSV 取込済みの書庫に metadata 付き JSON を重ねると、新規 0 件のまま既存アイテムに引用データが補完され、フォルダ割当は変化しない
16. カードにメモを書いて再読込しても残る。同じ CSV を再取込してもメモとフォルダ割当が消えない
17. メモを書いたまま一覧をスクロールしても（段階描画が走っても）編集中の入力欄と入力内容が消えない
18. エクスポートのプレビューが §3-1 の書式（`MM-DD ` 見出し・6スペース字下げ・`> ` 引用・`@handle URL`）になり、`twitter.com` が残らない
19. コピーすると対象が「未出力 0 件」になり、カードに「→ YYYY-MM-DD 出力済」が出る。メモを編集すると「出力後に編集」に変わり、「出力後に編集したもの」1件として再び出せる。「直前の印を取り消す」で元に戻る
20. 1,913字の長文で引用が既定150字＋`…` に切れ、絵文字が割れない。0字にすると引用行が消える。メディアのみのポストは「（本文なし・メディアのみ）」になる。ゴミ箱のアイテムは対象に含まれない
21. バックアップ JSON を書き出したあとメモを編集し、同 JSON を取り込むと**編集後（新しい方）のメモが残る**（`nAt` 比較）

## 7. スコープ外

X API の利用、ログイン、引用元本文の取得・復元、t.co の展開、他人への公開・共有機能。
